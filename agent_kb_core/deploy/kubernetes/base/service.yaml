apiVersion: v1
kind: Service
metadata:
  name: agent-kb-core
  namespace: agent-kb
  labels:
    app.kubernetes.io/name: agent-kb-core
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: agent-kb-core
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
