# 项目审计纪要 —— 2026-08-28（grill-me 审问产出）

> 定位判定：**B（团队工具）**——给组内同事真实使用，非个人原型（A），暂不产品化（C）。
> 此定位重排了全部缺口优先级：语料覆盖、真实使用、答案可信度为主线；安全/多租户后置。

## 一、审问确定的决策（8 项）

| # | 决策 | 内容 | 触发条件/状态 |
|---|------|------|--------------|
| 1 | 定位 B | 团队工具；安全层/RBAC/多租户**后置**，webui 明示"无鉴权仅限 Tailscale 内网" | 已定 |
| 2 | 语料策略 C | 先做「语料↔骨架映射分析」，用数据决定进库范围；反对全量进库（Athena 10K 源码会稀释语义通道） | 待执行 |
| 3 | 映射技术 C | 两级流水：词法粗筛（21K 全跑）→ 语义精排（本机嵌入，numpy 全配对）→ 未映射桶人工抽查；**粒度到 1348 张节点卡** | 待执行 |
| 4 | 进库路径 C | 本轮**只产映射表不进库**：`corpus_node_mapping.jsonl`（文件/节点/分数/信号来源/人工确认状态）；A/B 去重冲突（同内容双计印证分）未设计前不批量进库 | 待执行；进库延后 |
| 5 | 反馈闭环 C | **冻结**（feedback 表与 evaluate_feedback 已备）。触发条件：**webui 被 3+ 同事真实使用一周** → 做 👍/👎 按钮。反对隐式反馈（噪声大无校准） | 冻结中 |
| 6 | 最小可用入口 A+C | ①webui 注入 RemoteJSONEmbeddingProvider（读环境变量，~10 行）接通语义通道；②`--host 0.0.0.0` + Tailscale 共享；③README 三步启动。**明确不做**：一键打包/注册服务（等有人用）、鉴权 | 待执行（本次） |
| 7 | 答案评测 B+缩水A | ①**契约门**（确定性，进 pytest）：答案必须引用 evidence_id、partial 必须披露缺口、sufficient 不许说不知道、长度约束；②**5 黄金问题事实锚点**（确定性字符串断言，零 token）：如 DCDC 保护答案必含 过压/过温/短路 | 待执行（本次） |
| 8 | 固化 C | 本审计文档 + 每项完成后各自提交 | 本文档即产出 |

## 二、明确放弃/后置项（防过度工程锚点）

| 项 | 放弃理由 | 重启条件 |
|---|---------|---------|
| 安全层/RBAC 接线 | B 定位不需要；代码已备 | 定位转 C（对外部署） |
| 反馈按钮 UI | 无真实流量，激活=拟合噪声 | 3+ 同事用一周 |
| 隐式反馈推断 | 无显式信号校准 | 反馈按钮有数据后 |
| 动态查询文本（实验B） | 融合态 soup 已优，收益不定 | 向量通道成为主导通道时 |
| self_max 归一 | 两轮消融数据否决 | 换嵌入后端后重跑消融再说 |
| 一键启动打包 | 无用户基数 | 同事开始用 webui |

## 三、已知技术债（未排期）

- **嵌入后端不可互比**：fastembed(ONNX) 与 Ollama 同模型余弦仅 0.63~0.91——换后端必须重锚基线（门禁文档 §12）；
- 内存基线 retrieve ~1s（29528 evidence 纯扫描），可倒排优化，未排期；
- `graph_extraction_runs=0`：DeterministicRelationExtractor 未在生产库跑过，图边全靠骨架灌入——抽取器要不要跑、跑出什么质量，未评估；
- CI 只跑 pytest，三门禁依赖本地环境（嵌入服务/DB），未入 CI；
- corpus_node_mapping 完成后：骨架可能需补节点/别名（映射表的副产品输入）。

## 四、下一步执行序列（按依赖排序）

1. webui 接语义通道 + host 0.0.0.0 + README（问题 6）→ 提交
2. 答案契约门 + 5 黄金锚点 pytest（问题 7）→ 提交
3. 语料映射分析脚本（问题 2/3/4）→ 映射表落盘 → 人工抽查 → 提交
4. 触发条件监控：待同事真实使用后回看「反馈按钮」「一键启动」触发器