# V0.3 Migration Plan（只设计，不执行）

- Document ID: AKB-DD-V03-016 · Status: Design Only

## §26 七问

1. schema 变化：2 新表（akb_evidence_sets/akb_synthesis_runs）+3 索引（2 UNIQUE+1 status）；
   既有表零改动（provenance.activity 枚举扩展随 13 号 ADDITIVE 处理）；
2. 无 migration 能否先跑？——可以：synthesis 管线的纯函数部分（对齐/冲突/权重）可在
   无新表的临时结构上验证；但**持久化/幂等/provenance 全链**需要 13 号——实现任务按
   "迁移先行"执行；
3. EvidenceSet/SynthesisRun = **canonical persisted**（幂等锚+审计聚合，非 derived）；
4. 索引：ux_akb_sets_fingerprint（Set 锚）/ ux_akb_synruns_fingerprint（Run 锚，NULL 允许）/
   ix_akb_synruns_status；
5. 向后兼容约束：纯新增表；V0.1/V0.2 读写路径零触碰；DROP 即回滚（只被 V0.3 写入）；
6. 可逆：是（新表无既有数据破坏面；备份先行惯例）；
7. backfill：不需要（无历史数据语义变更）。

## 生产安全

默认不执行；工具复用 SchemaMigrator（DRY-RUN 默认 + --apply + --verify）；
与 V0.2 migration 12 / V0.1 graph_edges 回填同窗待批。