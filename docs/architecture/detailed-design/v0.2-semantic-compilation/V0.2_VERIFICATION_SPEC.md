# V0.2 Verification Spec

- Document ID: AKB-DD-V02-011 · Status: Ready for Implementation
- 测试落位：`agent_kb_core/tests/v02_semantic_compilation/`
- 通则：离线（零 LLM/网络）；默认 provider=BuiltinRuleExtractor（strict）；每用例注明 Requirement/Invariant/Test ID

| Test ID | 名称 | Given/When/Expected | Invariant | RTM(V0.2) |
|---|---|---|---|---|
| CMP-001 | Evidence → SemanticUnit | 合法 evidence 经 compile → SemanticUnit 落库，normalized_text 按 N-01..08，五字段候选齐备 | INV-004 | V02-REQ-001 |
| CMP-002 | SemanticUnit → Candidate Assertion | unit 含完整 relation candidate → KnowledgeAssertion(status=candidate)，evidence_refs=[evidence_id] | INV-001 | V02-REQ-002 |
| CMP-003 | 重复编译幂等 | 同 evidence 同 config 两次 compile → 第二次 idempotent_hit=True；units/assertions 数量不变 | INV-005 | V02-REQ-003 |
| CMP-004 | 版本可区分 | 同 evidence 不同 compiler_version → 新 unit（不同 fingerprint），旧产物不动 | — | V02-REQ-004 |
| CMP-005 | Evidence 不可修改 | compile 前后 evidence content_hash/行数据不变（append-only 触发器兜底） | INV-005 | V02-REQ-005 |
| CMP-006 | Candidate 不自动 validated | 全部产物 status=candidate；后续 transition 仍走 V0.1 状态机 | INV-001/002 | V02-REQ-006 |
| CMP-007 | inferred 必有 derivation | provider 产 inferred 候选无 derivation → E-CANDIDATE-BUILD-FAILED；带完整 derivation → 创建成功且 derivation 非空 | INV-002 | V02-REQ-007 |
| CMP-008 | mapping 默认 candidate | 命中词表 → mapping_status=candidate；authoritative 映射不存在于 V0.2 | — | V02-REQ-008 |
| CMP-009 | provenance 完整 | 产物可回答：Evidence/compilation run/compiler version/extraction method/ontology version/provider/actor/policy 八问 | INV-004 | V02-REQ-009 |
| CMP-010 | Provider neutrality | 更换 provider实现（builtin→fake provider）不改 Canonical 结构/表/assertion 语义；无 provider 字段泄漏 | ADR-009 | V02-REQ-010 |
| CMP-011 | Determinism | 同输入两次 run（排除审计字段）输出全等（JSON 逐字节）；候选顺序稳定 | — | V02-REQ-011 |
| CMP-012 | Failure isolation | 注入 segment 级失败 → 其余 segment 继续；run 警告记录；evidence/authoritative 零修改 | INV-005 | V02-REQ-012 |
| CMP-013 | Malformed provider output | fake provider输出缺字段/超范围 confidence → E-SEMANTIC-EXTRACTION-FAILED，无 unit/assertion产生 | — | V02-REQ-013 |
| CMP-014 | Unknown ontology quarantine | 词表外 concept → quarantined；unit 落库但无 assertion；run.warnings 有记录 | — | V02-REQ-014 |
| CMP-015 | Golden compilation regression | golden/semantic_compilation/ 全量跑：产物指纹稳定 + 全部判定符合 expected | — | V02-REQ-015 |

## Golden Dataset 计划（任务书 §21）

新增 `docs/verification/golden/semantic_compilation/`（不动现有 golden 30 案例）：

| 组 | 数量 | 内容 |
|---|---|---|
| positive | 20 | 参数/约束/定义/测试/条件/观测/时间区间/版本取代等句式，每案例：evidence 文本 + expected units/relations/assertions（semantic 级断言，非字节快照） |
| negative | 10 | 空 evidence/纯表格无语义/malformed provider/unknown ontology/inferred 无 derivation/非法 confidence/重复 fingerprint/非法 actor/quarantine 全单元/run 中断恢复 |
| determinism | 5 | 同输入双跑全等（排除审计字段）/候选顺序稳定/舍入一致/时间锚定不用时钟/配置 hash 区分 |
| provider-boundary | 5 | fake provider 换装不破坏 canonical/malformed 拒绝/provider 记录完整/provider 隔离失败/provider actor 无法治理跃迁 |

**总计 40 案例**，schema `semantic_compilation_case.schema.json`（versioned，Draft 2020-12），
validator `tools/validate_semantic_compilation_golden.py`（expected semantic 结构对比，非字节 diff）。
现有 Golden 30 案例 expected values 零改动。