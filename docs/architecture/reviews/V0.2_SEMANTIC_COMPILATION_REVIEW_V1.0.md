# V0.2 Semantic Compilation Design Review V1.0

- Review ID: AR-DD-V02-001 · Date: 2026-09-03 · Base: fc65370
- Scope: V0.2 Semantic Compilation Core Detailed Design Package（14 文件）对 V0.1 已接受基线的边界一致性

## Files Reviewed

detailed-design/v0.2-semantic-compilation/ 全 13 文档 + RTM V0.2 +
交叉核对（V0.1 DB Design / STATE_MACHINE / INTERFACE / MIGRATION / INVARIANT_REGISTRY / ADR-001..010）

## Review Checklist（任务书 §23 十三项）

| # | 检查项 | 结论 | 证据 |
|---|---|---|---|
| 1 | Canonical boundary | ✅ | KnowledgeAssertion 定义未动；V0.2 只产 candidate |
| 2 | Evidence boundary | ✅ | Evidence immutable 全层只读；CMP-005/append-only 兜底 |
| 3 | SemanticUnit semantics | ✅ | DM-004 具体化；新增 4 列逐条论证（任务书 §7/§14/§16） |
| 4 | Candidate Assertion semantics | ✅ | 唯一出口 create_candidate；assertion_type 四值；CMP-006 |
| 5 | Provenance | ✅ | CompilationRun + 三层留痕；CMP-009 八问 |
| 6 | determinism | ✅ | 两级定义 + D-01..07；strict/provider 不混淆 |
| 7 | idempotency | ✅ | fingerprint UNIQUE（数据层）+ 幂等返回（应用层） |
| 8 | provider neutrality | ✅ | Protocol 边界；CMP-010；canonical 零 provider 字段 |
| 9 | legacy adapter boundary | ✅ | KB1 链并行不直连；facts.py 经验以"移植泛化"进内置 provider |
| 10 | migration impact | ✅ | ADDITIVE 12 号迁移设计；V0.1 表零改写；只设计不执行 |
| 11 | database consistency | ✅ | Consistency Matrix 16/16（终态；首轮评审时为 12/12 pre-repair state） |
| 12 | security boundary | ✅ | provider 无治理接口；malformed 拒绝；CMP-013 |
| 13 | agent write boundary | ✅ | 无新写路径；V0.1 静态审计测试保持 |

## Issues Found

| # | Severity | Issue | Resolution |
|---|---|---|---|
| R2-01 | P2 | 初稿 pipeline 未明确 Temporal 锚定禁用当前时钟（确定性漏洞） | 已修：T-02 锚定 document_effective_time，T-06 无时间合法 |
| R2-02 | P2 | CompilationRun 状态缺 partial（部分 segment 失败语义） | 已修：status CHECK 含 partial |
| R2-03 | P2 | fingerprint UNIQUE 与存量 NULL 行冲突风险 | 已修：UNIQUE 允许多 NULL（SQLite 语义）+ 存量不回填论证 |

**P0 = 0 · P1 = 0 · P2 = 3（全部修复）**

## Remaining Risks

1. R-01..R-06 内置规则覆盖面对 OBC/DCDC 语料的召回率未知——Golden 40 案例将给出首版量化；
2. Semantica adapter 属 implementation 阶段评估（V0.2 design 只留 Protocol 位）；
3. 生产 migration（12 号）与 V0.1 graph_edges 回填同窗执行（待批准）。

## Decision

```text
APPROVED
V0.2 Semantic Compilation Design = READY FOR IMPLEMENTATION
（Consistency Matrix 16/16 ALL CONSISTENT；P0=0 P1=0；首轮 12/12 为 pre-repair 历史状态）
```

---

# Consistency Repair Round（AKB-V02-DESIGN-002 · 2026-09-03）

三必须修复（任务书 §3）全部落实并交叉同步：

| Repair | 内容 | 落点 |
|---|---|---|
| A — Fingerprint 构造 formalize | 字符串拼接式 → CanonicalJSON（V0.1 同款 dumps 约定）+ 六条形式化性质（字段名固定/顺序规范化/无隐式拼接/无 locale/无时钟/字节级比较）+ fingerprint_spec=v1 | DETERMINISM_SPEC §fingerprint；INTERFACE/MIGRATION/主详设/Matrix 引用同步；CMP-017/018 |
| B — 单证据 vs batch 歧义消除 | V0.2 一次 invocation 恰一 Evidence 为 canonical API；batch=OUT OF SCOPE（V0.3+）；evidence_ids_json 恒单元素（future-compat 审计 schema 保留）；多证据 → 拒绝创建 run | INTERFACE §7；MIGRATION runs 注释；主详设；CMP-016/019 |
| C — 术语归一 | 旧名 `IntegrationRun` → `CompilationRun`（唯一实体名；"integration" 仅保留系统集成语境） | Review 记录行修正；全包扫描零残留 |

新增设计级验证用例（不实现）：CMP-016（单证据契约）/ CMP-017（Canonical 序列化）/
CMP-018（碰撞抗性编码规则）/ CMP-019（run evidence 基数）。
RTM 增补 V02-REQ-016/017 两行（含 SRS/INV 溯源）。
Consistency Matrix 扩至 16/16 主题。

## 复核结论（§7 要求逐项记录）

- P0 = 0 · P1 = 0（无未解决语义不一致）
- Repair A/B/C 三项修复已实际反映于全部受影响文档（交叉引用核对通过）
- V0.2 scope = single-evidence compilation（batch 显式出界至 V0.3+）
- canonical fingerprint serialization = CanonicalJSON（fingerprint_spec=v1，字节级比较）
- CompilationRun 术语全包归一（旧名 `IntegrationRun` 实体引用零残留，仅本修复记录中作为历史名提及；系统集成语境的 "integration" 用法不受影响）
- CMP 编号 001..019 连续无冲突；RTM V02-REQ-001..017 映射完整
- Production code changed: 0 · V0.1 implementation changed: 0 · Git diff = documentation only

## Final State（AKB-V02-DESIGN-003 终态确认）

```text
Consistency Matrix = 16/16
CMP = 001..019
P0 = 0
P1 = 0
READY FOR IMPLEMENTATION
```

（历史值仅以 pre-repair 标注保留：首轮 12/12 / CMP-001..015 见上文 R2 轮次记录。）

## Final Decision

```text
APPROVED
V0.2 Semantic Compilation Design
READY FOR IMPLEMENTATION
```
