# V0.5 Release — Knowledge Graph Layer（Freeze Marker）

- Date: 2026-09-04 · FINAL baseline: **7c19186**
- Status: **ACCEPTED / FROZEN**

## Freeze Baseline

| 阶段 | SHA |
|---|---|
| Design（frozen） | f5e28dd |
| IMPL-001 Graph Schema | 11bed68 |
| IMPL-002 Entity Resolution | 28721cc |
| IMPL-003 Graph Persistence | ae982a7 |
| IMPL-004 Graph Query | aca64cf |
| Evidence backfill | 7c19186 |
| **Final Release** | 见 git log（docs(v0.5): finalize release baseline） |

## Test Summary（FINAL 实测重取）

```text
V0.1 Golden        = 30/30 PASS
V0.2 Golden        = 40/40 PASS
V0.3 Golden        = 55/55 PASS
V0.4 reasoning     = 21/21 PASS
V0.5 GS            = 7/7 PASS
V0.5 ER            = 16/16 PASS
V0.5 GP            = 25/25 PASS
V0.5 GQ            = 25/25 PASS
agent_kb_core      = 346 passed + 1 skipped（347 total，0 failures/errors）
full repository    = 345 passed + 1 skipped（exit 0）
CI                 = Agent KB Core CI run 33975167058 SUCCESS（8 jobs）+
                     Tests run 33975167073 SUCCESS @ 7c19186
（1 skipped = V0.3 capped 数据场景设计性跳过，历史记录非 V0.5 引入）
```

## Known P2（accepted，不阻断 freeze）

1. **P2-01** Q-04 inference chain max_depth = 8——对齐设计 DC-06（frozen spec 字面）；
2. **P2-02** Q-02 neighborhood 边语义 = relates_to 双向 + supports 出向——
   frozen spec §7 Q-02 字面范围；
3. **P2-03** query limit 上限 1000——frozen spec 未定义分页；
4. **P2-04** determinism 语义 = 同一数据库状态确定性（事件溯源 source_id 跨库
   重放不保证 ID 相等——IMPL-001 起的 mint_id 属性，非 V0.5 缺陷）；
5. **P2-05** kg_* 表无分页 API（同 P2-03——留给 V0.6 change-control）。

## Known Limitations

- LLM/外部 reasoner、graph-aware reasoning、DomainPack runtime、语义相似度自动合并、
  向量检索、外部图数据库：全部不在 V0.5 范围（scope audit 零越界）；
- Entity merge/split/alias 治理模型为内存态 + akb_provenance 审计事实
  （IMPL-002 既定设计——持久治理队列表属 V0.6+ 候选）。

## Database Safety

- Production DB = untouched（prod-isolation fingerprint PASS；全部验证内存库）；
- migration chain V0.1→V0.5 完整（15 个 migrations，版本唯一；migration 15 幂等重放；
  V02/V03 重复引用行为历史 append-only 修复，FINAL 零重写）。

## Legacy Isolation

`agent_kb.graph`（V0.1 legacy）零修改零隐式桥接：API 面互斥实测 +
graph_edges 表零变化 + test_phase6/7 回归 PASS。

## Release Scope / Out of Scope

- IN：六类 Node + 六类 Edge 确定性投影/持久化/只读查询、L1 identity + 治理红线、
  invalidation/flagged 语义、五级 provenance 回溯、rebuild 幂等；
- OUT：graph-aware reasoning / LLM / DomainPack runtime / 外部图 DB / 向量检索 /
  semantic auto-merge / 分布式（全部 V0.6+ 候选，需新任务书）。

## Freeze 规则

本文件签署后 `docs/V0.5/` 的 frozen specification（五设计文档）不再修改；
语义变更必须走新 change-control（V0.6+ 任务书）。