# V0.1 Evidence Core — Implementation Verification Report

- Task: AKB-V01-IMPL-001 + AKB-V01-HARDENING-001 · Date: 2026-09-01/02 · Branch: rebuild/agent-kb-core
- Implementation Base Commit: d9cd66f · Hardening Base: ab9d83e
- Design Baseline: V0.1 Detailed Design Package（READY FOR IMPLEMENTATION，P0=0/P1=0）

## 1. Implementation Base Commit

`d9cd66f docs: repair v0.1 detailed design consistency`

## 2. Migration Version

- 迁移版本 **10**（`v01_evidence_core`，23 语句）经仓库现有 `SchemaMigrator` 框架落地；
- 顺带修复上游遗漏：`PHASE9_MIGRATIONS(v9)` 已定义但未纳入 `CORE_MIGRATIONS` → 已纳入；
  同步修正 `PLATFORM_MIGRATIONS = CORE_MIGRATIONS`（原 CORE+PHASE9 组合在 v9 入 CORE 后
  会重复应用 version 9 → UNIQUE 冲突，7 个 phase 测试炸）；
- 编号依据：生产库 schema_migrations 实际状态（1..8 已应用），代码内 v9 存在 → 下一个合法号=10。

## 3. Files Added

```text
agent_kb_core/src/agent_kb/evidence_core/__init__.py        包导出
agent_kb_core/src/agent_kb/evidence_core/ids.py             ID 工厂（mint_id/content_hash/migration_assertion_id）
agent_kb_core/src/agent_kb/evidence_core/models.py          7 模型（row↔domain↔canonical_json 零损）
agent_kb_core/src/agent_kb/evidence_core/store.py           EvidenceStore + LegacyEvidenceResolver
agent_kb_core/src/agent_kb/evidence_core/resolver.py        resolver 再导出（任务书结构）
agent_kb_core/src/agent_kb/evidence_core/state_machine.py   状态机（合法矩阵/权限矩阵/INV 规则纯函数）
agent_kb_core/src/agent_kb/evidence_core/assertions.py      AssertionStore/Validator/Provenance（SAVEPOINT 原子迁移）
agent_kb_core/src/agent_kb/evidence_core/graph.py           GraphProjection（PATH A/B 分离 + integrity 巡检）
agent_kb_core/src/agent_kb/evidence_core/migration/__init__.py
agent_kb_core/src/agent_kb/evidence_core/migration/v01_evidence_core.py  迁移工具（DRY-RUN 默认/--apply/--resume/--verify）
agent_kb_core/tests/v01_evidence_core/conftest.py           内存库 fixture（含 legacy evidence 表模拟）
agent_kb_core/tests/v01_evidence_core/test_evidence_store.py
agent_kb_core/tests/v01_evidence_core/test_assertion_store.py
agent_kb_core/tests/v01_evidence_core/test_assertion_validator.py
agent_kb_core/tests/v01_evidence_core/test_state_machine.py
agent_kb_core/tests/v01_evidence_core/test_transition_audit.py
agent_kb_core/tests/v01_evidence_core/test_provenance.py
agent_kb_core/tests/v01_evidence_core/test_graph_projection.py
agent_kb_core/tests/v01_evidence_core/test_migration.py
agent_kb_core/tests/v01_evidence_core/test_idempotency.py
agent_kb_core/tests/v01_evidence_core/test_security_boundaries.py
agent_kb_core/tests/v01_evidence_core/test_golden_regression.py
docs/verification/v0.1/V0.1_IMPLEMENTATION_VERIFICATION_REPORT.md（本文件）
docs/verification/v0.1/V0.1_IMPLEMENTATION_SUMMARY.html
```

## 4. Files Modified

```text
agent_kb_core/src/agent_kb/storage/migrations.py            +Migration v10；v9 入 CORE；PLATFORM=CORE
agent_kb_core/tests/test_phase6_production.py               schema_version 断言 8→10（迁移集演进）
agent_kb_core/tests/test_phase7_hardening.py                同上
agent_kb_core/tests/test_phase8_deployment.py               同上
agent_kb_core/tests/test_phase8_r2_operations.py            同上
```

（测试断言修正性质说明：schema_version 数字是迁移集快照；v9/v10 入 CORE 是已接受基线的
合法演进，数字断言按新迁移集更新并在行内注明，无 invariant 降低。）

## 5-6. Tests Added / Passed

V0.1 专项 **44 passed**（11 文件），覆盖 Verification Spec 全部 18 用例 ID：
EVD-001/002 · AST-001/002/003/004/005/006/007 · PROV-001 · GRAPH-001A/001B/002 ·
MIG-001/002/003 · REG-001（+G016 冲突检出/hypothesized 边界/legacy resolver 边界等补充）。

全量回归三入口（收尾实测）：

```text
python agent_kb_core/tools/validate_golden_dataset.py → PASS（30/30/0）
python -m pytest agent_kb_core/tests -q → 132 passed
python -m pytest -q                      → 132 passed
```

## 7. Negative Tests Passed

无证据 validated 拒绝（INV-001）/ inferred 缺 derivation 拒绝（API+DB 双层，INV-002）/
inferred→asserted 拒绝 / hypothesized→validated|asserted 拒绝 / validated→candidate 拒绝 /
rejected 终态 / 直创 asserted 拒绝 / 越权 actor 拒绝 / 直写 SQL UPDATE status 拒绝（触发器）/
不可变列 UPDATE 拒绝（触发器）/ 审计行 UPDATE/DELETE 拒绝（触发器）/ evidence UPDATE/DELETE 拒绝 /
未知 evidence 引用拒绝 / 未知 document 拒绝 / 空 content 拒绝 / 投影 candidate 拒绝（PATH B）/
链路断裂 E-CHAIN-BROKEN —— **17 条负例全过**。

## 8. Invariant Verification

| Invariant | 机制 | 测试 |
|---|---|---|
| INV-001 | DB CHECK + Validator | AST-002 ✓ |
| INV-002 | DB CHECK + validate_creation | AST-004 ✓ |
| INV-003 | assertion_ref 列 + 回填 100% | GRAPH-001A/B ✓ |
| INV-004 | 四级 trace 链 | PROV-001/EVD-002 ✓ |
| INV-005 | append-only 触发器×6 + 受控更新触发器×2 | AST-006/007 ✓ |
| INV-006 | 投影失败仅回滚投影 | GRAPH-002 ✓ |
| INV-007 | create_candidate 统一入口（llm: 前缀无特权） | AST-001 ✓ |
| INV-008 | evidence_core 无 agent 写 API；静态 import 审计 | security_boundaries ✓ |
| INV-009/010 | NOT IN SCOPE（V0.1 无 Memory/Action，不得标"已实现"） | — |

## 9. Golden Verification

validator PASS（30 cases/0 invalid/0 dup）；REG-001 通过；golden `evd:node:*` 引用经
legacy resolver 影子映射可解析（REG-001b）。

## 10. Regression Verification

- 三入口全绿（132 passed ×2 + validator PASS）；
- run_production_health PASS（5 变体 + 判定契约 5/5）——生产库迁移后投影面零回归；
- run_skeleton_health / run_retrieval_health 未受影响（无共享表改动）。

## 11. Production DB Status

**CHANGED（additive schema only）**——如实记录：
- 触发路径：`test_answer_contract.py` 使用生产库跑 `query_production_store`，而
  `production_context` 构造时执行 `SchemaMigrator.migrate()` → V0.1 合入后迁移 10
  在该路径被应用（v9 亦在此前未应用状态下被一并应用）；
- 变更内容：7 张空 akb_ 表 + graph_edges 加可空列 assertion_ref + 触发器/索引；
  **既有 KB1 表与数据零改动**（ADDITIVE FIRST）；
- 影响评估：三入口回归全绿 + run_production_health PASS → 既有行为零回归；
- 遗留：生产库 graph_edges.assertion_ref **未回填**（467 条边 assertion_ref=NULL）——
  按 MIGRATION_PLAN 属迁移工具 `--apply` 职责，**未经架构负责人批准不在本任务执行**
  （工具默认 DRY-RUN，符合任务书 §20）；
- 数据回填与"生产库迁移由测试触发"的流程问题（测试应使用隔离库）已列入 Outstanding Risks。

## 12. Outstanding Risks

1. 生产库 graph_edges 待回填（工具就绪，等批准）；
2. 测试触发生产库迁移的路径应改为隔离测试库（AG-006 CI 化的一部分）；
3. SAVEPOINT 原子性依赖 SQLite 版本行为，已在 DB_DESIGN 注明"实现期微调但守卫语义不变"；
4. migration tool 的 `--verify` 门禁（broken_refs/INV 双检）已实现，建议纳入 CI（AG-006）。

## 13. Release Decision

**READY FOR RELEASE（代码与设计基线）** —— 任务书 §32 checklist：
migration/EvidenceStore/AssertionStore/Validator/state machine/transition audit/provenance/
graph assertion_ref/idempotency/rollback/negative/Golden/full regression/INV-001..008 全 PASS；
no Semantica leakage ✓（静态扫描）；no provider leakage ✓；INV-009/010 = NOT IN SCOPE（如实标注）。

## 14. Commits

三个逻辑 commit（implementation / migration+backfill 工具 / verification），见 git log。

---

# Release Hardening（AKB-V01-HARDENING-001，2026-09-02）

## 1. Production DB Isolation（session-level proof，AKB-V01-ACCEPTANCE-001 重做）

- **原缺陷**（hardening 版）：tempfile 快照文件依赖测试执行顺序、不保证 session 首尾覆盖——
  无法严格证明整个 pytest session 前后生产库未变。
- **重做**：`conftest_prod_isolation.py` 经 `pytest_plugins` 注册——
  `pytest_sessionstart` 记录指纹（SHA-256 / schema_version / 5 张关键表计数 /
  graph_edges 列集 / size+mtime）→ 整个 session → `pytest_sessionfinish` 重读对比；
  任何差异 → 输出 before/after diff 并使会话失败（无 silent pass）；
  豁免仅当 `validation/.prod_db_isolation_exemptions.json` 存在（默认无），
  且豁免在报告中显式声明——skipped ≠ PASS。
- **skip 语义**（§3）：CI 无生产库 → isolation 测试 `NOT EXECUTED (skipped)`，
  报告不写成 PASS；本地有生产库 → 真实 PASS。
- **negative proof**（§4）：`test_pii_002` —— 对隔离副本执行 `SchemaMigrator.migrate()`，
  断言副本 schema 如预期演进（migration 应用）且**生产库指纹逐字段不变**——
  验证的是隔离机制本身，而非仅仅"当前生产库恰好没变"。
- **answer contract 强约束**（§5）：`test_pii_003` 静态审计——
  `db_path=DB` 直连生产库的写法为 0，必须经 `isolated_db` fixture（副本）。
- **test evidence**：11 passed（isolation 4 + answer contract 7，副本）；
  实测报告 `verdict=PASS, diff={}`，before/after SHA-256 一致
  （9f81e3ceff742576…），schema_version=10。
- **production DB before/after**：same file / same hash / same schema / same counts ✅（session 级）

## 2. Adversarial Governance Tests（test_governance_adversarial.py，9 项全过）

| 攻击路径 | 场景 | 结果 |
|---|---|---|
| 1 伪造完整链 | 直 SQL INSERT provenance+transitions+UPDATE status | **语义裁决**：DB 层强制状态对白名单/type 边界/provenance 一致性/append-only；actor 授权属应用层（SQLite 无认证主体）。持 DB 写权限者的伪造链会成功但**完整留痕**（append-only 审计行不可篡改，事后可追责）；API 层重放为幂等 no-op，进一步晋升仍受权限矩阵约束。残余风险：持 DB 写权限者=DBA 等价，由部署边界防护 |
| 1b 假 provenance | transitions 引用不存在的 provenance | 11.2 触发器 ABORT ✅ |
| 1c 非法状态对 | validated→candidate 伪造迁移行 | 11.1 白名单触发器 ABORT ✅ |
| 2 伪造 actor | system/llm 冒充 human promote | API 权限矩阵 E-ACTOR-NOT-AUTHORIZED ✅ |
| 2b type 边界（SQL 层） | inferred→asserted 伪造迁移行 | 11.1b type-boundary 触发器 ABORT ✅ |
| 2c hypothesized（SQL 层） | →validated 伪造迁移行 | 同上 ABORT ✅ |
| 3 provenance_ref 操纵 | 无审计改写 provenance_ref | 11.4 触发器 ABORT ✅ |

**最小安全修复**（migration 11 `v01_governance_hardening`，4 触发器，ADDITIVE）：
11.1 状态对白名单 / 11.1b type 边界（inferred→asserted、hypothesized→validated|asserted）/ 
11.2 provenance 存在性 / 11.3 provenance_ref 一致性 / 11.4 无审计 provenance_ref 改写守卫。
**未删除任何触发器，未把安全性退回纯 Python。**

## 3. CI Verification（与 Local Verification 严格区分）

| | Local（本机，Windows/Py3.14） | GitHub CI（ubuntu，隔离环境） |
|---|---|---|
| validator | PASS | PASS（run 33645020731，job v01-evidence-core ×3.11/3.12/3.13） |
| V0.1 专项 | 51 passed | PASS ×3 |
| agent_kb_core 全量 | 135 passed | PASS ×3 |
| 仓库全量 | 140 passed（+isolation/负例 5） | PASS ×3 |
| run id | — | 33645020731（push）/ 33645029559（PR）均 success |

### 最终 Acceptance CI Evidence 矩阵（AKB-V01-ACCEPTANCE-001 §8 重定义）

| Layer | Result |
|---|---|
| V0.1 dedicated CI（agent-kb-core.yml） | **PASS**（run 33645020731 + 33645029559，V0.1 job ×3 versions success） |
| agent_kb_core suite | **PASS**（135 passed local；CI 同套件 ×3 success） |
| repository-wide local suite | **PASS**（140 passed，含 isolation/负例） |
| original GitHub Tests workflow（tests.yml） | **PASS（修复后实证）**：root cause = 仓库根无 pyproject.toml/setup.py（`pip install -e "file:///D:/a/KB/KB"` ERROR，job 100299283574 日志实测）——**2026-07 a538722 清理根 pyproject 以来的 baseline failure，非 V0.1 引入**。最小修复：install 改 `pip install -e "agent_kb_core[dev]"`、删除引用已清理资产的 4 个死 job、integration 空 marker 容忍（run_integration_marker.py）。修复后 run 33707156860/33707152844 = **success** |
| overall GitHub CI | **SUCCESS（全部 required checks）** @ 9862cce：Agent KB Core CI run 33707156858 = success（V0.1 job ×3 + pytest ×3 + gates ×2），Tests run 33707156860 = success（test ×3 + integration） |

CI job：`V0.1 Evidence Core verification`——干净环境、无生产库（isolation 测试自动 skip）、
四命令与本地等价（validator → v01 专项 → core 全量 → repo 全量）、失败即 non-zero、无 allow-failure。

## 4. 更新后的测试统计

```text
V0.1 tests:      51 passed（44 + adversarial 7）
Golden:          30/30
agent_kb_core:   135 passed
repository full: 135 passed
production DB isolation: PASS
adversarial tests: 9 passed（含 6 条 DB 层 ABORT 实证 + 2 条语义裁决 + 1 条 API 边界）
GitHub CI: success（run 33645020731，V0.1 job ×3 + pytest ×3 + gates ×2）
```


---

# Final Acceptance Evidence Repair（AKB-V01-ACCEPTANCE-001，2026-09-03 收口）

## §2 session-level isolation 重做

`conftest_prod_isolation.py`（经 tests/conftest.py import 绑定注册）：
`pytest_sessionstart`（tryfirst）指纹入 session 状态 → `pytest_sessionfinish`（trylast）对比，
diff 非空 → 输出 before/after 并使会话失败；豁免文件默认不存在。
报告落盘 `validation/.prod_db_isolation_report.json`（gitignore）。

- **本地实测**：`verdict=PASS, diff={}`，before/after SHA-256 一致（9f81e3ceff742576…）。
- **CI 无库分支**：PROD_AVAILABLE=False → hooks 定义为 no-op + `VERDICT = NOT_EXECUTED`；
  isolation 测试显式 `skip`（NOT EXECUTED）——**skipped 不冒充 PASS**（§3 语义锁定）。
- **negative proof**（§4）：test_pii_002 对副本 migrate → 副本 schema 演进 + 生产库指纹逐字段不变。
- **answer contract 强约束**（§5）：test_pii_003 静态审计 `db_path=DB` 直连 = 0。

## 修复轮次实录（CI 逐轮实证，非一次到位）

| Push | 两 workflow 结果 | Root cause（日志实测） |
|---|---|---|
| 48fd169 | Tests failure + AgentCore failure | ①legacy：根 pip install 无 pyproject（baseline）；②V0.1 job 新增 repo-wide 步骤踩 `pytest_plugins in non-top-level conftest`（exit 4） |
| b2b840d | AgentCore failure（V0.1 suite + pytest 矩阵）/ Tests：test ×3 success | `pytest_plugins` 修复后 CI 无库分支缺 hooks → `cannot import name 'pytest_sessionfinish'` |
| 638abac | AgentCore **success** / Tests failure（仅 integration） | pwsh 引号解析炸 `python -c` 内联 wrapper |
| c57ac2c→9862cce | **双 workflow success** | wrapper 移至 `agent_kb_core/tools/run_integration_marker.py` |

## 最终验证数字（local + CI 双轨）

```text
V0.1 tests:      55 passed（44 核心 + 9 adversarial + 2 isolation 拆分增量；CI V0.1 job ×3 PASS）
Golden:          30/30 PASS
agent_kb_core:   143 passed（root entry，含 isolation）
repository full: 143 passed
production DB isolation: PASS（session-level，本地实证；CI = NOT EXECUTED 如实标注）
adversarial tests: 9 passed
GitHub CI @ 9862cce:
  Agent KB Core CI run 33707156858 = success（8 jobs）
  Tests run 33707156860 = success（4 jobs：test ×3.11/3.12/3.13 + integration-tests）
overall GitHub CI = SUCCESS（全部 required checks 实证，非本地转述）
```
