# V0.2 Semantic Compilation Core — Implementation Verification Report（Final）

## 1. Document Control

- Document ID: AKB-V02-VER-001（Final）· Status: **RELEASE BASELINE EVIDENCE**
- Date: 2026-09-03 · Branch: rebuild/agent-kb-core
- 本版为**终版重写**（替代初版 IMPL-001 报告 + IMPL-002 hardening 附录）——
  历史提交保留于 git 历史，本文档只陈述终态。
- 声明：**Initial implementation acceptance was superseded by implementation hardening.
  Final acceptance is based on the hardened state.**（初版验收被 hardening 取代；
  最终验收基于加固后状态。）全文恰有一个终态决定（§18）。

## 2. Baselines（immutable，git 可溯）

| 阶段 | SHA | 内容 |
|---|---|---|
| Architecture | 6189a8f | V0.2 Design APPROVED / READY FOR IMPLEMENTATION |
| Implementation | 1d24a13 | feat(v0.2): implement semantic compilation core |
| Verification tests | a002e09 | test(v0.2): add semantic compilation verification |
| Golden alignment | 6f20c9b | fix(v0.2): refine builtin extractor rules |
| Golden dataset | e812ce6 | feat(v0.2): add semantic compilation golden dataset 40 cases |
| Initial evidence | 25dd9d6 | docs(v0.2): add implementation verification evidence |
| Hardening | 1685224 | fix(v0.2): harden temporal and run invariants |
| Final documentation | 707ded4 | docs(v0.2): record hardening evidence |

## 3. Scope

V0.2 = Evidence → SemanticUnit → Candidate KnowledgeAssertion 的确定性可审计编译。
实现位置 `agent_kb/evidence_core/compilation/`（8 层，复用 V0.1 storage/治理设施）。
Out of scope：batch 编译（V0.3+）/Agent Runtime 集成/Semantic Graph/LLM provider 实现/
生产 migration 执行/schema 重设计。

## 4. Design Compliance

Design ↔ Implementation 交叉核查 **PASS**（23 锚点程序化验证，2026-09-03 终验）：
DETERMINISM（四键 CanonicalJSON/spec=v1/锚语义五条）· VERIFICATION_SPEC（CMP-001..023
行齐全且测试名对应）· MIGRATION_PLAN（列/表/索引/锚注记）· INTERFACE（compile 单证据
签名/describe_run/trace）· PIPELINE（L1..L8 全层类对应）· NORMALIZATION（N-01..08）·
E-R（R-01..06）· TEMPORAL（T-01..06/五类时间字段/零时钟语义）· ONTOLOGY（O-01..06）·
ERROR_MODEL（8 码全实现）。本轮发现并修复：VERIFICATION_SPEC 补 CMP-020..023 行
（hardening 轮次遗漏回写——文档缺口，非实现缺口）。

## 5. Implementation Verification

模块清单与要点：errors.py（8 错误码 + IdempotentHit 幂等返回语义）/ models.py（10 dataclass
+ canonical_json）/ normalizer.py（L1 span 分段 + L2 N-01..08）/ providers.py（Protocol +
BuiltinRuleExtractor R-01..06 strict + Fake provider）/ resolvers.py（L4/L5 稳定排序无孤儿 +
L6 五类时间分离 + L7 quarantine）/ compiler.py（L8 唯一出口 create_candidate + CanonicalJSON
fingerprint + SAVEPOINT 原子 + provenance 三层留痕）。

## 6. Hardening Verification（AKB-V02-IMPL-002，三缺陷闭环）

| Defect | 修复 | 测试 |
|---|---|---|
| A：document_effective_time=None 硬编码 | evidence 查询 JOIN akb_documents.effective_at 作锚 | T-A1/A2/A3 |
| B：fingerprint 锚语义隐式 | 五条锚语义成文；幂等返回经 compiler_run_ref 解析完整 run 产物 | CMP-020 |
| C：单证据契约仅签名级 | 入口 str 守卫（列表/多值串 → E-COMPILER-INVALID-EVIDENCE）；伪造多元素 run 行被基数校验拒 | CMP-021 |

## 7. CMP-001..023 Results

```text
CMP-001..019（初版 19 用例）           全 PASS
CMP-020 多段指纹锚                     PASS
CMP-021 多证据拒绝边界（行为级）        PASS
CMP-022 run 基数完整性（库级全查）      PASS
CMP-023 provenance trace 确定性        PASS
T-A1/A2/A3（temporal anchor）          PASS
V0.2 tests total: 28 passed
```

## 8. Temporal Anchor Verification

T-A1 相对表达锚定 akb_documents.effective_at=2026-01-01T00:00:00Z ✓；
T-A2 双跑语义时间字段全等（fingerprint 恒定）✓；T-A3 缺 effective_at → unresolved、
无伪造当前日期 ✓。代码层验证：resolvers/compiler 零 datetime.now/time.time 语义参与（静态核查）。

## 9. Fingerprint Anchor Verification

CMP-020：恰 1 锚 unit（非锚 NULL 合法）/ 全 unit 同 compiler_run_ref / 幂等重放返回完整 run
产物（units+assertions 同集）/ 无第二 create_candidate。锚语义五条在
DETERMINISM_SPEC/MIGRATION_PLAN/compiler.py 三处一致成文。

## 10. Single-Evidence Boundary Verification

CMP-021 行为级：列表注入 → 入口守卫 E-COMPILER-INVALID-EVIDENCE（持久层零绑定风险）；
多值字符串 → 同拒；伪造多元素 evidence_ids_json run 行（持久层注入）→ 基数校验拒绝、
不得作为合法 V0.2 run。全部注入零副作用（runs/units/assertions/evidence 前后一致）。
公开 API 无 batch 参数（签名审计）。

## 11. CompilationRun Integrity

CMP-022：成功 run `len(evidence_ids_json)==1` 且 `[0]==invocation id`；
库级全查 `json_array_length!=1` 计数=0、`json_extract!=unit.evidence_id` 计数=0；
无 unit 引用多 Evidence run。

## 12. Provenance Verification

Assertion → SemanticUnit → CompilationRun → Evidence(+Document) 四级链在多 segment 场景
正确解析父级；两次 trace 结果等价（deterministic）；run 失败亦留 provenance 痕（append-only）。

## 13. Golden Verification

```text
V0.2 semantic_compilation: 40/40 PASS（positive 20 / negative 10 / determinism 5 /
                            provider-boundary 5；semantic structure 对比）
V0.1 Golden 30:            PASS（零改动，完全兼容）
```

## 14. V0.1 Regression

```text
Golden 30 validator:   PASS（exit 0）
agent_kb_core 全量:    171 passed（143 V0.1 面 + 28 V0.2 面）
repository 全量:       171 passed（python -m pytest -q，exit 0）
run_production_health: PASS（V0.1 查询通道零回归）
production DB isolation: PASS（session 指纹机制未受影响）
```

## 15. CI Evidence（GitHub Actions，独立环境）

```text
HEAD 707ded4：
  Agent KB Core CI run 33752624808 = SUCCESS
    V0.1 Evidence Core verification (3.11/3.12/3.13) = success ×3
    pytest (3.11/3.12/3.13) = success ×3
    container and deployment gate = success
    operational recovery and readiness gate = success
  Tests run 33752624849 = SUCCESS
```
不依赖本地计数——CI 独立环境实证。

## 16. Production DB Safety

- 全部验证在内存库/临时库；生产库零触碰（isolation session 指纹实证不变）；
- Migration 12 已验证 fresh/存量/重复三场景，**生产执行未发生**（待架构负责人批准）；
- V0.1 graph_edges 467 条回填同状态（待批准，可与 migration 12 同窗）。

## 17. Residual Risks（全部 non-blocking）

1. BuiltinRuleExtractor 全语料召回率未量化（Golden 40 为首个锚点，V0.3 扩充）；
2. 单证据单事务在超长 Evidence 下为大事务（batch 化时需分批——V0.3 议题）；
3. 幂等 assertions 反查为 LIKE 非索引最优（语义正确，量级可接受）；
4. Semantica adapter 留 Protocol 位（ADR-006 不变，实现属后续评估）；
5. 生产 migration 执行窗口待批准（操作风险非代码风险）。

## 18. Final Acceptance Decision

```text
P0 = 0
P1 = 0
P2 = 5（全部 documented / non-blocking，见 §17）

FINAL DECISION: ACCEPTED — V0.2 Semantic Compilation Core = RELEASE BASELINE
（唯一终态决定；依据 = hardened state + §7-15 全部证据）
```

## 19. Commit Evidence

```text
6189a8f design baseline → 1d24a13 impl → a002e09 tests → 6f20c9b golden alignment →
e812ce6 golden dataset → 25dd9d6 initial evidence → 1685224 hardening →
707ded4 hardening evidence → fc…（final baseline commit，见 git log）
```