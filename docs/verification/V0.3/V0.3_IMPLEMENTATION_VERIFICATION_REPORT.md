# V0.3 Multi-Evidence Synthesis — Implementation Verification Report

- Document ID: AKB-VER-V03-IMPL-001 · Date: 2026-09-03 · Branch: rebuild/agent-kb-core
- Design Baseline: f4a5705（V0.3 APPROVED / DESIGN READY FOR IMPLEMENTATION）
- V0.2 Baseline: 310f345（ACCEPTED — RELEASE BASELINE）· V0.1: ACCEPTED / FROZEN

## 1-3. Document Control / Design Baseline / Implementation Scope

实现位置 `agent_kb/evidence_core/synthesis/`（8 模块：errors/models/evidence_set/alignment/
conflicts/weights/synthesizer/__init__）——与 compilation 平级，复用 V0.1 storage/治理设施。
11 阶段管线全部落地：Set 创建（canonical 序/指纹锚）→ Membership Validation →
Unit Retrieval（缺 unit 报错不补编译）→ Entity/Relation/Event/State Alignment（L1-L4 层级、
簇 ID 稳定编号）→ Temporal 六态 → Conflict 7 类（全溯源字段/capped）→ Compatibility 五级
（规则表+rule_audit）→ SourceWeight（weight≠adjudication）→ Candidate Synthesis
（唯一边界 create_candidate；恒 candidate）→ Provenance（五级链）→ 治理（V0.1 不变）。

## 4. Migration Result

Migration 13 `v03_multi_evidence_synthesis`：akb_evidence_sets（7 列+UNIQUE set_fingerprint）+
akb_synthesis_runs（16 列+status CHECK 含 capped+fingerprint UNIQUE）。fresh 内存库全量迁移 ✓；
V0.1/V0.2 既有表零改动；生产库未动（工具 DRY-RUN 模式，执行待批准）。

## 5-15. Verification（V03-CMP-001..025 + 对抗面）

```text
V0.3 dedicated tests: 31 passed（CMP-001..025 全覆盖 + 6 对抗/诊断）
CMP-001 Set 身份/复用                PASS    CMP-014 ontology conflict        PASS
CMP-002 顺序不变 [A,B]==[B,A]         PASS    CMP-015 候选合成（全成员 refs）   PASS
CMP-003 变更→新指纹/旧冻结            PASS    CMP-016 candidate-only           PASS
CMP-004 重复成员拒绝（零副作用）       PASS    CMP-017 provenance 五级链        PASS
CMP-005 实体对齐（跨证据簇）          PASS    CMP-018 确定性（双跑全等）        PASS
CMP-006 关系对齐                      PASS    CMP-019 幂等（无重复候选/run）    PASS
CMP-007 事件对齐                      PASS    CMP-020 失败隔离（缺 unit→failed）PASS
CMP-008 状态对齐                      PASS    CMP-021 provider 边界            PASS
CMP-009 时间六态                      PASS    CMP-022 基数 1..32 边界          PASS
CMP-010 compatible                    PASS    CMP-023 向后兼容（V0.2 compile）  PASS
CMP-011 value conflict（双方保留）     PASS    CMP-024 无 authoritative 写       PASS
CMP-012 temporal conflict             PASS    CMP-025 trace 确定性             PASS
CMP-013 source conflict+权重          PASS
对抗面：capped（128 注入）/provider crash 隔离/provenance 原子/逆序幂等/直写拦截/
V0.2 compile 不变——全 PASS
```

实现期修复的内部缺陷（全部属实现细节，无设计变更）：unit-scoped candidate_id 键
（V0.2 candidate_id 仅 run 内唯一——跨 unit 冲突导致对齐错串）、relation 簇键改
(subject_cluster, predicate)（object 差异进 members 才能检出 VALUE_CONFLICT）、
conftest sys.path 污染（v03 conftest 不再插 tests 目录，消除 v01 显式 import 冲突）、
单证据 relation 不参与合成（S-01 语义：合成只发生在跨证据簇）。

## 16-18. Regression & Golden

```text
V0.1 Golden 30:      PASS（exit 0）
V0.2 Golden 40:      PASS 40/40（exit 0）
V0.3 Golden 55:      PASS 55/55（结构/配额/字段完备性 validator exit 0）
V0.1/V0.2 回归:      全 PASS（V0.2 专项 28 + V0.1 全套保持）
```

## 19. Repository Test

```text
agent_kb_core 全量: 202 passed
repository 全量:    202 passed（python -m pytest -q，exit 0）
（V0.1 143 面 + V0.2 28 面 + V0.3 31 面）
```

## 20. CI Evidence（@ 最终 HEAD，push 后实测）

```text
Agent KB Core CI = SUCCESS（8 jobs：V0.1 verification ×3.11/3.12/3.13 + pytest ×3 + gates ×2）
Tests workflow = SUCCESS
```

## 21. Production DB Safety

生产库零触碰（全部验证内存库）；migration 13 未在生产执行；
无 EvidenceSet/SynthesisRun 生产创建；V0.1/V0.2/V0.3 生产隔离指纹机制保持。

## 22. Residual Risks（non-blocking）

1. 对齐规则表聚类精度未全语料量化（Golden 55 为首锚）；
2. MAX_SET_SIZE=32/MAX_CONFLICTS=128 为设计默认（配置化可调）；
3. 冲突值确定性选取（字典序最大）为 V0.3 简化——治理复核才是裁决正道；
4. provider 对齐建议通道预留（builtin 主路径不依赖）；
5. 生产 migration 13 执行窗口待批准（可与 12/graph_edges 回填同窗）。

## 23. Final Decision

```text
ACCEPTED — IMPLEMENTATION BASELINE
V0.3 Multi-Evidence Semantic Synthesis Core
（§37 全部 gate：P0=0 P1=0 / CMP-001..025 PASS / Golden 55+40+30 PASS /
 V0.1/V0.2/V0.3 tests PASS / repository PASS / Provenance ✓ / Determinism ✓ /
 Idempotency ✓ / Provider boundary ✓ / Candidate-only ✓ / Production safety ✓ / CI ✓）
```

## 24. Commits

```text
f633ef1 feat(v0.3): implement multi-evidence synthesis core
f718b9d test(v0.3): add multi-evidence synthesis verification
fd8b216 feat(v0.3): add synthesis golden dataset
827b5d3 docs(v0.3): record multi-evidence synthesis implementation baseline
efabef8 fix(v0.3): align synthesis alignment keys with unit-scoped candidates
e300475 test(v0.3): align cluster id assertion with design determinism
（最终 CI 取证 @ e300475：Agent KB Core CI 33780769920 SUCCESS（8 jobs）+ Tests
  33780769896 SUCCESS；2025…（全量 202 passed 本地双入口））
```