# V0.3 Implementation Hardening Report

- Document ID: AKB-VER-V03-HARDEN-001 · Date: 2026-09-03 · Base: 3716f92
- 状态演进：REPAIR REQUIRED（独立评审发现对齐层风险）→ 全部缺陷修复 + H-001..015 行为实证 →
  终态见 §7

## 1. Defect Register

| Defect | 描述 | Severity | 状态 |
|---|---|---|---|
| A | Relation alignment 使用外层循环变量 u["unit_id"]（ambient 依赖） | P1 | FIXED |
| B | state_clusters = [] 占位——State Alignment 未实现 | P1 | FIXED |
| C | 实体对齐单键选择（L1 OR L3 OR L4）而非层级多键；L3 type 级 ref 作 union 键致同 type 实体误并 | P1 | FIXED |
| D | relation 簇 (subject, predicate) 键的 object 语义完整性核查 | P2 | AUDITED PASS |
| E | 派生对象 ambient loop state 依赖（通用规则） | P1 | FIXED（A 同源） |
| F | STATE_CONFLICT 未接入 ConflictDetector | P1 | FIXED |
| G | compatibility 优先级未显式（冲突可能被弱规则覆盖成 COMPATIBLE） | P1 | FIXED |
| H | temporal "all anchored → same" 过粗（六态失真） | P1 | FIXED |
| I | event 簇未强制 participant overlap | P1 | FIXED |

## 2. Root Cause（逐缺陷）

- A/E：V0.2 candidate_id 仅 run 内唯一（跨 unit 重名）——对齐层原实现 `u["unit_id"]`
  引用外层 for-unit 循环变量，在 relation 循环中失效（unit_id 全部串到最后一个 unit）；
- B：初版实现以最小可用路径交付 CMP-008 面但 state 簇为空占位——违反"不得以占位宣称 PASS"；
- C：L3 ontology_ref 是 TYPE 级引用（object_type:equipment）非实例唯一——作 union 键
  会让所有同 type 实体合并；且单键选择使"含 ref 的成员"与"仅 normalized_form 的成员"
  分裂；
- G：初版规则序 INVALID→CONFLICTING→COMPATIBLE 无 PARTIALLY 分层且无显式序声明；
- H：`len(anchored)==len(units) → same` 未比较锚值/区间关系；
- I：event 簇只按 event_time 分组。

## 3. Code Change（before → after）

| Defect | before | after |
|---|---|---|
| A/E | `s_cl = ec_index.get((u["unit_id"], r.get(...)))`（u 为外层循环变量） | relation record 自带 `unit_id`；`s_cl = ec_index.get((r["unit_id"], r.get(...)))`——零 ambient 依赖 |
| B | `result.state_clusters = []` | `_state_alignment()`：(subject_cluster, state predicate family, valid_time)；六种 alignment 值（aligned/contradictory/missing/partial-anchored…）；contradictory 窗 → contradictions[] |
| C | 单键 `{L1 or L3 or L4}` | 多键 union-find：L1 精确形 + L2 归一形（含尾部系动词剥离 L2b）+ L4 别名；L3 降权为审计快照（type 级不作 union 键）；entity_type 分歧 → IDENTITY_CONFLICT 源 |
| F | 无 STATE_CONFLICT | ConflictDetector 消费 alignment.state_contradictions → STATE_CONFLICT（detection_method=CONF-006-STATE，双方 sides 保留） |
| G | 隐式顺序 | 显式五级优先序 INVALID > CONFLICTING > PARTIALLY_COMPATIBLE > COMPATIBLE > INCOMPARABLE（COMPAT-002 规则号 + inputs/result 记录） |
| H | `len(anchored)==len(units) → same` | 六态真实判定：same（锚值全等）/overlapping（区间相交）/sequential（全隔）/missing（全缺）/unresolved（存在未解析）/partial（混合） |
| I | 仅 event_time 分组 | event_time 相等 AND 参与实体簇重叠 ≥1（并查合并；无重叠 → 独立事件） |

## 4. Behavioral Verification（H-001..015 + 既有回归）

```text
H-001 relation unit ownership isolation（三 unit 值各异全归其主）   PASS
H-002 duplicate candidate_id across units（三值互异 sides=3）       PASS
H-003 state alignment positive（同簇兼容窗 aligned）                PASS
H-004 state alignment negative（缺锚不伪造）                        PASS
H-005 state conflict（contradictory → STATE_CONFLICT 双方保留）     PASS
H-006 temporal interval states（same/overlapping/sequential/missing/unresolved）PASS
H-007 event same-time different-participant rejection              PASS
H-008 entity hierarchy consistency（同实体合一簇/无关不合）          PASS
H-009 relation value conflict（265V/280V sides 保留；同值合成对照）  PASS
H-010 deterministic cluster numbering（全簇序稳定无重号）            PASS
H-011 reversed EvidenceSet idempotency（[A,B]≡[B,A] 同指纹）        PASS
H-012 provenance ownership（unit→document 全链无歧义）              PASS
H-013 candidate-only boundary（恒 candidate+直写拦截）              PASS
H-014 compatibility precedence（冲突成员必 CONFLICTING）            PASS
H-014b/c unrelated subjects / unknown state predicate              PASS
H-015 no ambient unit dependency（重名候选+逆序重放值归属不变）       PASS
```

## 5. Regression Verification

```text
V0.3 suite:        48 passed（31 CMP 面 + 17 hardening）
V0.2 Golden 40:    PASS 40/40
V0.1 Golden 30:    PASS 30/30
V0.3 Golden 55:    PASS 55/55（结构/配额 validator）
agent_kb_core:     219 passed
repository-wide:   219 passed（exit 0）
CI（GitHub Actions @ HEAD）: Agent KB Core CI SUCCESS（8 jobs）+ Tests SUCCESS
```

## 6. Residual Risk

1. L2b 尾部系动词剥离（是/为/均）为对齐层归一——语义等价性在中文工程语料上待全量验证；
2. state predicate family 为内置最小集（has_parameter/constrained_by/observed_value），
   DomainPack 扩展接口已留；
3. temporal overlapping/sequential 判定基于 valid_time 区间端点（闭区间语义）——
   开/闭端精确语义随治理复核演进；
4. 生产 migration 13 执行窗口仍待批准。

## 7. Final Hardening Decision

```text
P0 = 0 · P1 = 0 · P2 = 4（全部 documented / non-blocking）
全部缺陷 A..I 修复且行为实证（H-001..015）+ 回归零破坏（219 passed 三入口）。
HARDENING COMPLETE → V0.3 IMPLEMENTATION BASELINE（ACCEPTED 状态由终版报告确认）
```